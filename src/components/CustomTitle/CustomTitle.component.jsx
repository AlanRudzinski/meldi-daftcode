import React from 'react';

import { StyledCustomTitle } from './CustomTitle.styles'

const CustomTitle = ({ children }) => (
    <StyledCustomTitle>{children}</StyledCustomTitle>
)

export default CustomTitle;